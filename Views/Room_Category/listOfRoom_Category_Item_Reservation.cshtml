@model IEnumerable<Hotel_Management_System.Models.Room_Category_Item_Reservation>

@{
    ViewBag.Title = "listOfRoom_Category_Item_Reservation";
    Layout = "~/Views/Shared/_AdminPanelLayout.cshtml";
}

<h2>listOfRoom_Category_Item_Reservation</h2>

<p>
    @Html.ActionLink("Create New", "addRoom_Category_Item_Reservation")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.RCI_Reservation_Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reservation.Reservation_members)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>

            @Html.DisplayFor(modelItem => item.RCI_Reservation_Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reservation.Reservation_members)
        </td>
        
        <td>
            @Html.ActionLink("Edit", "ditRoom_Category_Item_Reservation", new { id=item.RCI_Reservation_Id }) |
            @Html.ActionLink("Details", "detailOfRoom_Category_Item_Reservation", new { id=item.RCI_Reservation_Id }) |
            @Html.ActionLink("Delete", "removeRoom_Category_Item_Reservation", new { id=item.RCI_Reservation_Id })
        </td>
    </tr>
}

</table>
